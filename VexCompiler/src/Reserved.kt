enum class Reserved(val word: String) {
    ON("on"),
    STATE("state"),
    GOTO("goto"),
    SCREEN("screen"),
    REPEAT("repeat"),
    IF("if"),
    ELSE("else"),
    BY("by"),
    EACH("each"),
    MAKE("make"),
    RESET("reset"),
    REMOVE("remove"),
    TO("to")
}
