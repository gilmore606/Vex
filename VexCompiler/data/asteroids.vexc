controls:
	left (switch)
	right (switch)
	thrust (switch)
	start (button)
	fire (button debounce 200)
	hyperspace (button debounce 1000)

to utilFunction:
    a = a + (4 * b) + 7 * (foo^2)
    b = (a * 1.5) / 3
    c = 4 * 12 + 25
    alive = !true || (hasLives && (x > 7))

to mangle:
    result = trystuff(4, "test", 56, vec(5,5), vec(3,3))
